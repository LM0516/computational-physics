\section{Numerical Integration}

\subsection{Newton-Cotes Integration}

\begin{exercise}\label{ex:integrals}
	For each integral below, use the trapezoidal rule to estimate the integral
	for $m=10\cdot 2^k$ nodes for $k=1,2,\ldots,10$. Make a log-log plot of the
	errors and confirm or refute second-order accuracy.
	\begin{enumerate}
		\item $\displaystyle \int_0^1 x\log(1+x)\, dx = \frac{1}{4}$

		\item $\displaystyle \int_0^1 x^2 \tan^{-1}x\, dx = \frac{\pi-2+2\log 2}{12}$

		\item $\displaystyle \int_0^{\pi/2}e^x \cos x\, dx = \frac{e^{\pi/2}-1}{2}$

		\item $\displaystyle \int_0^1 {\tan^{-1}(\sqrt{2+x^2})\over
				      (1+x^2)\sqrt{2+x^2}}dx={5\pi^2\over 96}$

		\item $\displaystyle \int_0^1 \sqrt{x} \log(x) \, dx = -\frac{4}{9}$
		      (Hint: Although the integrand has the limiting value zero as $x\to 0$,
		      it cannot be evaluated naively at $x=0$. One possibility is to start the
		      integral at $x=\epsilon_{\rm mach}$. Alternatively, you can define a
		      wrapper function $g(x)$ that returns $g(0)=0$ and $g(x)=\sqrt{x}\log(x)$
		      otherwise.)

		\item $\displaystyle \int_0^1 \sqrt{1-x^2}\,\, dx = \frac{\pi}{4}$
	\end{enumerate}
\end{exercise}

\begin{solution}
	To evaluate the given definite integrals, we employ the composite
	trapezoidal rule. For an interval $[a, b]$ discretized into $m$ equally
	spaced sub-intervals with step size $h = \frac{b-a}{m}$, the composite
	trapezoidal rule approximations are given by:
	\[
		\int_a^b f(x) \, dx \approx \frac{h}{2} \left( f(x_0) +
		2\sum_{i=1}^{m-1} f(x_i) + f(x_m) \right)
	\]
	where $x_i = a + i h$. The number of nodes was systematically increased
	according to $m = 10 \cdot 2^k$ for $k = 1, 2, \ldots, 10$, yielding a
	maximum of $10,240$ nodes.

	\paragraph{Results}
	Table~\ref{tab:integral_results} summarizes the final numerical
	approximations achieved at $k=10$ ($10,240$ nodes) alongside their
	theoretical exact values.

	\begin{center}
		\centering
		\captionof{table}{Comparison of exact analytical solutions and numerical
			approximations evaluated at $m = 10,240$ nodes.}
		\label{tab:integral_results}
		\begin{tabular}{clll}
			\toprule
			\textbf{Integral} & \textbf{Exact Value}   & \textbf{Numerical Approximation} & \textbf{Absolute Error} \\
			\midrule
			1                 & $0.25$                 & $0.2500000009482$                & $9.48 \times 10^{-10}$  \\
			2                 & $\approx 0.210657251$  & $0.2106572528715$                & $1.87 \times 10^{-9}$   \\
			3                 & $\approx 1.905238690$  & $1.9052386790888$                & $1.14 \times 10^{-8}$   \\
			4                 & $\approx 0.514041897$  & $0.5140418956028$                & $1.86 \times 10^{-9}$   \\
			5                 & $\approx -0.444444444$ & $-0.4444422428639$               & $2.20 \times 10^{-6}$   \\
			6                 & $\approx 0.785398163$  & $0.7853978796777$                & $2.84 \times 10^{-7}$   \\
			\bottomrule
		\end{tabular}
	\end{center}

	\begin{center}
		\centering
		\includegraphics[width=0.9\linewidth]{figures/5-1/newton-cotes-results.pdf}
		\captionof{figure}{Evaluation of the integrals as a function of the
			number of nodes $m$. The exact analytical solutions are denoted by
			dashed horizontal lines.}
		\label{fig:results}
	\end{center}

	\begin{center}
		\centering
		\includegraphics[width=0.9\linewidth]{figures/5-1/newton-cotes-absolute-error.pdf}
		\captionof{figure}{Log-log progression of the absolute error relative to
			the number of nodes $m$. Note the disparate convergence behaviors
			between smoothly behaving integrands (1-4) and those with singular
			derivatives (5-6).}
		\label{fig:abs_error}
	\end{center}

	\paragraph{Discussion \& Convergence Analysis}
	The error bound for the composite trapezoidal rule is theoretically given by:
	\[
		E \leq \frac{(b-a)}{12} h^2 \max_{x \in [a,b]} |f''(x)| = \mathcal{O}(h^2)
	\]
	This stipulates that the algorithm demonstrates second-order accuracy
	\textit{only} if the integrand $f(x)$ is twice continuously differentiable,
	$f \in C^2[a,b]$.

	As observed in Table~\ref{tab:integral_results} and
	Figure~\ref{fig:abs_error}, all the integrals align with theoretical
	expectations, exhibiting strict second-order accuracy. In a log-log plot of
	error versus nodes $m$, this manifests as a line with a slope of $-2$.

\end{solution}

\begin{exercise}
	For each integral in Exercise~\ref{ex:integrals} above, apply the Simpson
	formula and compare the errors to fourth-order convergence.
\end{exercise}

\begin{solution}
	To evaluate the convergence improvements over the trapezoidal rule, we apply
	the composite Simpson's rule to the same set of integrals. For an interval
	$[a, b]$ discretized into $m$ equally spaced sub-intervals (where $m$ is even)
	with step size $h = \frac{b-a}{m}$, the approximation is given by:
	\[
		\int_a^b f(x) \, dx \approx \frac{h}{3} \left[ f(x_0) + 4
			\sum_{i=1,3,5\ldots}^{m-1} f(x_i) + 2 \sum_{i=2,4,6\ldots}^{m-2} f(x_i) +
			f(x_m) \right]
	\]
	Theoretical error analysis states that the absolute error for Simpson's rule
	is bounded by:
	\[
		E \leq \frac{(b-a)}{180} h^4 \max_{x \in [a,b]} |f^{(4)}(x)| =
		\mathcal{O}(h^4)
	\]
	This implies a theoretical fourth-order convergence ($\mathcal{O}(m^{-4})$),
	provided that the integrand is at least four times continuously
	differentiable, $f \in C^4[a,b]$. The integrals were evaluated using
	double-precision arithmetic (\texttt{Float64}) up to $m = 10,240$ nodes.

	\paragraph{Results}
	Table~\ref{tab:simpson_results} details the absolute errors obtained at the
	maximum node count ($m = 10,240$) for both the trapezoidal and Simpson's
	rules.

	\begin{center}
		\centering
		\captionof{table}{Comparison of absolute errors between the Trapezoidal and Simpson's
			rules evaluated at $m = 10,240$ nodes.}
		\label{tab:simpson_results}
		\begin{tabular}{clll}
			\toprule
			\textbf{Integral} & \textbf{Simpson Approximation} & \textbf{Trapezoidal Error} & \textbf{Simpson Error} \\
			\midrule
			1                 & $0.2500000000000007$           & $9.48 \times 10^{-10}$     & $7.00 \times 10^{-16}$ \\
			2                 & $0.2106572512258067$           & $1.87 \times 10^{-9}$      & $2.00 \times 10^{-16}$ \\
			3                 & $1.9052386904826724$           & $1.14 \times 10^{-8}$      & $2.60 \times 10^{-15}$ \\
			4                 & $0.5140418958900695$           & $1.86 \times 10^{-9}$      & $1.20 \times 10^{-15}$ \\
			5                 & $-0.4444440750259714$          & $2.20 \times 10^{-6}$      & $3.69 \times 10^{-7}$  \\
			6                 & $0.7853981242237340$           & $2.84 \times 10^{-7}$      & $3.91 \times 10^{-8}$  \\
			\bottomrule
		\end{tabular}
	\end{center}

	\begin{center}
		\centering
		\includegraphics[width=0.9\linewidth]{figures/5-1/newton-cotes-comparison-absolute-error.pdf}
		\captionof{figure}{Log-log progression of the absolute error relative to the
			number of nodes $m$ using Simpson's rule. The smooth integrands rapidly
			descend to the machine precision threshold, whereas the singular integrands
			stall at sub-optimal fractional convergence.}
		\label{fig:simpson_error}
	\end{center}

	\paragraph{Discussion \& Convergence Analysis}
	The integration results perfectly delineate the strict mathematical
	requirements of Simpson's rule:

	\begin{itemize}
		\item \textbf{Smooth Integrands (Integrals 1--4):} These functions satisfy
		      $f \in C^4[a,b]$. Because Simpson's rule scales at $\mathcal{O}(h^4)$, the
		      truncation error diminishes so rapidly that it essentially hits the
		      \texttt{Float64} machine epsilon limit ($\approx 2.22 \times 10^{-16}$) at a
		      fraction of the maximum nodes. Further increases in node count merely
		      accumulate numerical round-off noise.
		\item \textbf{Singular Integrands (Integrals 5--6):} The fourth derivative
		      $f^{(4)}(x)$ approaches infinity at the boundaries for both
		      $\sqrt{x}\log(x)$ and $\sqrt{1-x^2}$. Consequently, the $\mathcal{O}(h^4)$
		      theoretical bound is entirely invalidated. Simpson's rule offers negligible
		      practical improvement over the trapezoidal rule for these functions,
		      remaining trapped at fractional convergence rates due to the dominant root
		      singularities.
	\end{itemize}

\end{solution}

\subsection{Free-nodes Integration}

\begin{exercise}
	Write a program that computes the nodes and weights for the Gauss-Legendre
	quadrature in the interval $[-1,1]$. Below are some tips for the
	implementation.

	\begin{itemize}
		\item To find the roots $x_k$, with $k=1,\ldots,n$ of the Legendre
		      polynomial $P_n(x)$ of order $n$ use Newton's method. Take as the initial
		      condition for the $k$th root,
		      \[
			      x^{(0)}_k=\cos(\phi_k)\,,
			      \qquad
			      \phi_k={4k-1\over 4n+2}\pi\,.
		      \]
		\item In order to use Newton's method you need to evaluate $P_n(x_k^{(i)})$
		      and the derivative $P'_n(x_k^{(i)})$, where $x_k^{(i)}$ is the $i$th
		      iteration in the search for the $k$th root $x_k$. You can obtain these by
		      first solving the recursion relation,

		      \[
			      (m+1)P_{m+1}(x) = (2m+1)xP_{m}(x) - mP_{m-1}(x)\,,
		      \]

		      for $m=1,\ldots,n-1$, where $P_0(x) = 1$ and $P_1(x) = x$, with
		      $x=x^{(i)}_k$. The derivative is then computed using the relation,

		      \[
			      (x^{2}-1)P'_{n}(x)=n[xP_{n}(x)-P_{n-1}(x)]\,.
		      \]

		\item Once you reach the root $x_k$ within some tolerance, the value of the
		      derivative $P'_n(x_k)$ allows you to obtain the corresponding weight using

		      \[
			      w_k= {2\over (1-x_k^2)[P'_n(x_k)]^2}\,.
		      \]
	\end{itemize}

\end{exercise}

\begin{solution}
	The Gauss-Legendre quadrature optimally approximates the integral of a
	function over $[-1, 1]$ using a specific set of points (nodes) and
	corresponding weights. The nodes $x_k$ correspond to the roots of the $n$-th
	degree Legendre polynomial, $P_n(x) = 0$.

	The initial guesses $x_k^{(0)}$ are distributed according to the roots of the
	Chebyshev polynomials, which cluster near the boundaries $[-1, 1]$ and provide
	an excellent starting approximation for Legendre roots. The iteration
	terminates when successive approximations converge within machine precision
	($\approx 10^{-15}$).

	\paragraph{Results \& Validation}
	The algorithm was executed for quadrature orders $n \in \{2, 3, 4, 5\}$. To
	ensure numerical stability and precision, the computed sets of $(x_k, w_k)$
	were benchmarked against the standard \texttt{GaussQuadrature.jl} library. The
	custom implementation successfully matched the verified library routines to an
	absolute tolerance of $\mathcal{O}(10^{-14})$.

	Table~\ref{tab:gauss_legendre} details the evaluated roots and weights.

	\begin{center}
		\centering
		\captionof{table}{Computed Gauss-Legendre quadrature nodes and weights for
			$n=2,3,4,5$. Results match the \texttt{GaussQuadrature} library within
			$10^{-14}$ accuracy.}
		\label{tab:gauss_legendre}
		\begin{tabular}{crr}
			\toprule
			\textbf{Order ($n$)} & \textbf{Node ($x_k$)}    & \textbf{Weight ($w_k$)} \\
			\midrule
			2                    & $\pm 0.5773502691896257$ & $1.0000000000000000$    \\
			\midrule
			3                    & $0.0000000000000000$     & $0.8888888888888888$    \\
			                     & $\pm 0.7745966692414834$ & $0.5555555555555556$    \\
			\midrule
			4                    & $\pm 0.3399810435848563$ & $0.6521451548625462$    \\
			                     & $\pm 0.8611363115940526$ & $0.3478548451374538$    \\
			\midrule
			5                    & $0.0000000000000000$     & $0.5688888888888889$    \\
			                     & $\pm 0.5384693101056831$ & $0.4786286704993665$    \\
			                     & $\pm 0.9061798459386640$ & $0.2369268850561890$    \\
			\bottomrule
		\end{tabular}
	\end{center}

\end{solution}

\subsection{Advantage topics in integration}

\begin{exercise}
	For each integral, compute approximations using Gauss-Legendre quadrature
	rules with $n=4,6,8,\ldots,40$. Plot the error $|I_n-I|$ as a function of $n$ on
	a semi-log scale.
	\begin{enumerate}
		\item $\int_{-1}^1 e^{-4x}\, dx = \sinh(4)/2$
		\item $\int_{-1}^1 e^{-9x^2} = \sqrt{\pi}\, \operatorname{erf}(3)/3$
		\item $\int_{-1}^1 \operatorname{sech}(x) \, dx = 2 \tan^{-1}[ \sinh (1) ]$
		\item $\int_{-1}^1 \frac{1}{1+9x^2}\, dx = \frac{2}{3} \tan^{-1}(3)$
		\item $\int_{\pi/2}^{\pi} x^2 \sin 8x \, d x = -\frac{3 \pi^2}{32}$
	\end{enumerate}
\end{exercise}

\begin{solution}

	To analyze the convergence characteristics of numerical integration, we
	extended the exercise to compare three distinct quadrature frameworks:
	Gauss-Legendre Quadrature (GLQ) and the Fejér rule.

	For a given analytical integral $I$, the numerical approximation with $n$
	nodes is denoted as $I_n$. The absolute error is defined as $E_n = |I_n - I|$.
	Computations were evaluated over an evenly spaced grid of nodes $n \in \{4, 6,
		8, \dots, 40\}$.

	\paragraph{Results}
	Table \ref{tab:results-glq} summarizes the final numerical approximations
	achieved at maximum node density ($n=40$).

	\begin{center}
		\centering
		\captionof{table}{Exact solutions versus numerical approximations at $n=40$}
		\label{tab:results-glq}
		\resizebox{\textwidth}{!}{
			\begin{tabular}{c r r r}
				\toprule
				\textbf{Integral} & \textbf{Exact Solution} & \textbf{Fejér}        & \textbf{Gauss-Legendre} \\
				\midrule
				1                 & $13.644958598563875$    & $13.644958598563875$  & $13.644958598563878$    \\
				2                 & $0.5908048988396807$    & $0.5908048988396811$  & $0.5908048988396809$    \\
				3                 & $1.7315389664793173$    & $1.7315389664793175$  & $1.7315389664793173$    \\
				4                 & $0.8326971815988362$    & $0.8326971821009688$  & $0.8326971815925093$    \\
				5                 & $-0.9252754126021273$   & $-0.9252754126021280$ & $-0.9252754126021260$   \\
				\bottomrule
			\end{tabular}
		}
	\end{center}

	\paragraph{Discussion and Conclusion}
	The convergence profiles behave strictly according to theoretical expectations
	for spectral methods. For strictly entire analytical functions, Gauss-Legendre
	rule exhibit exponential convergence, rapidly descending to the noise floor
	imposed by $\varepsilon_{\text{mach}}$. At this threshold, \textit{catastrophic
		cancellation} and inherent rounding errors dominate, causing the error to
	oscillate pseudo-randomly around $10^{-15}$.

	Integral 4, $f(x) = (1+9x^2)^{-1}$, represents a classically ill-conditioned
	scenario due to the presence of poles located at $x = \pm i/3$ in the complex
	plane. Consequently, the asymptotic convergence rate is constrained by the
	proximity of these poles to the integration domain, yielding a much shallower
	descent in absolute error compared to the preceding entire functions.

	\paragraph{Figures}
	The evolution of the absolute errors on a semi-logarithmic scale are illustrated below.

	\begin{center}
		\begin{tabular}{cc}
			\includegraphics[width=0.45\textwidth]{figures/5-3/clenshaw-curtis-plot-1.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/clenshaw-curtis-plot-2.pdf}   \\
			{\small Integral 1: $\int_{-1}^1 e^{-4x} dx$}                                  &
			{\small Integral 2: $\int_{-1}^1 e^{-9x^2} dx$}                                  \\[1em]
			\includegraphics[width=0.45\textwidth]{figures/5-3/clenshaw-curtis-plot-3.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/clenshaw-curtis-plot-4.pdf}   \\
			{\small Integral 3: $\int_{-1}^1 \operatorname{sech}(x) dx$}                   &
			{\small Integral 4: $\int_{-1}^1 \frac{1}{1+9x^2} dx$}                           \\
		\end{tabular}

		\vspace{0.5em}
		\includegraphics[width=0.45\textwidth]{figures/5-3/clenshaw-curtis-plot-5.pdf} \\
		{\small Integral 5: $\int_{\pi/2}^{\pi} x^2 \sin(8x) dx$}
	\end{center}
\end{solution}

\begin{exercise}
	For each improper integral, compute approximations using the proper
	double-exponential quadrature rule with $N=n/2$ and $n=4,6,8,\ldots,60$. Plot
	the errors as functions of $n$ on a semi-log scale.
	\begin{enumerate}
		\item $\int_{-\infty}^\infty \dfrac{1}{1+x^2+x^4}\, dx = \dfrac{\pi}{\sqrt{3}}$
		\item $\int_{-\infty}^\infty e^{-x^2}\cos(x)\, dx = e^{-1/4}\sqrt{\pi}$
		\item $\int_{-\infty}^\infty (1+x^2)^{-2/3}\, dx =
			      \dfrac{\sqrt{\pi}\,\Gamma(1/6)}{\Gamma(2/3)}$
		\item $\int_{0}^{\infty} {1\over 1+x^2}\,dx = \frac{\pi}{2}$
		\item $\int_0^\infty \frac{e^{-x}}{\sqrt{x}}\,dx = \sqrt{\pi}$
	\end{enumerate}
\end{exercise}

\begin{solution}

	Double-exponential (DE) quadrature is highly effective for evaluating improper
	integrals and integrals with endpoint singularities. The methodology relies on
	an analytic transformation $x = \phi(t)$ that maps the integration domain into
	$(-\infty, \infty)$, such that the integrand decays double-exponentially at the
	boundaries:
	\[
		f(\phi(t)) \phi'(t) \sim \exp(-C \exp(|t|)) \quad \text{as } t \to \pm \infty
	\]
	This exceptionally rapid decay allows the integral to be truncated to a finite
	interval and evaluated using the trapezoidal rule, yielding spectral-like
	convergence.

	For infinite domains $(-\infty, \infty)$ (Integrals 1--3), we apply the
	standard mapping:
	\[
		x(t) = \sinh\left(\frac{\pi}{2} \sinh(t)\right)
	\]
	For semi-infinite domains $(0, \infty)$ (Integral 4), we apply:
	\[
		x(t) = \exp\left(\frac{\pi}{2} \sinh(t)\right)
	\]
	Integral 5 features an algebraic singularity at the origin $\left(x^{-1/2} \to
		\infty \text{ as } x \to 0\right)$. To optimize numerical stability at this
	boundary, an alternative mapping is employed:
	\[
		x(t) = \exp(t - \exp(-t))
	\]

	\paragraph{Results}
	Table \ref{tab:de-results} compares the theoretical, exact integral values to
	the computed double-exponential quadrature values at $n = 60$.

	\begin{center}
		\centering
		\captionof{table}{Exact solutions vs. Double-Exponential Quadrature
			approximations at $n=60$ ($N=30$).}
		\label{tab:de-results}
		\resizebox{\textwidth}{!}{
			\begin{tabular}{c r r}
				\toprule
				\textbf{Integral} & \textbf{Exact Solution} & \textbf{Computed Approximation ($n=60$)} \\
				\midrule
				1                 & $1.8137993642342178$    & $1.8137993642334227$                     \\
				2                 & $1.3803884470431430$    & $1.3803884470431436$                     \\
				3                 & $7.2859519436627430$    & $7.2859519436627450$                     \\
				4                 & $1.5707963267948966$    & $1.5707963267948963$                     \\
				5                 & $1.7724538509055159$    & $1.7724538509055163$                     \\
				\bottomrule
			\end{tabular}
		}
	\end{center}

	\paragraph{Discussion and Conclusion}
	The results validate the exceptional power of double-exponential (DE)
	quadrature. By applying an appropriate nonlinear conformal map to shift the
	domain to $\mathbb{R}$, we achieve an extreme concentration of the transformed
	integrand near the origin, with doubly-exponential tail decay.

	As theoretically expected, the convergence rates displayed in the accompanying
	figures are steep. For all five integrands—including Integral 5, which possesses
	an inverse-square-root boundary singularity—the quadrature algorithm approaches
	the double-precision noise floor $\varepsilon_{\text{mach}}$ by $n \approx 30
		\sim 40$ (corresponding to $N \approx 15 \sim 20$). Beyond this optimal
	saturation point, attempting to add more quadrature nodes ($n > 40$) yields no
	further analytical benefit; rather, it introduces \textit{catastrophic
		cancellation} due to the accumulation of floating-point rounding errors.

	Furthermore, employing the alternative substitution $x(t) = \exp(t -
		\exp(-t))$ for Integral 5 successfully mitigated numerical instability near the
	singular origin $x=0$, enabling a convergence profile nearly identical to the
	strictly smooth analytic integrals.

	\paragraph{Figures}
	The absolute errors $|I_n - I_{\text{exact}}|$ are evaluated and plotted as
	functions of $n$ below.

	\vspace{1em}

	\begin{center}
		\begin{tabular}{cc}
			\includegraphics[width=0.45\textwidth]{figures/5-3/double-exponential-quadrature-1.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/double-exponential-quadrature-2.pdf}   \\
			{\small Integral 1: $\int_{-\infty}^\infty \frac{1}{1+x^2+x^4} dx$}                     &
			{\small Integral 2: $\int_{-\infty}^\infty e^{-x^2}\cos(x) dx$}                           \\[1.5em]
			\includegraphics[width=0.45\textwidth]{figures/5-3/double-exponential-quadrature-3.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/double-exponential-quadrature-4.pdf}   \\
			{\small Integral 3: $\int_{-\infty}^\infty (1+x^2)^{-2/3} dx$}                          &
			{\small Integral 4: $\int_{0}^{\infty} \frac{1}{1+x^2} dx$}                               \\
		\end{tabular}

		\vspace{1.5em}
		\includegraphics[width=0.45\textwidth]{figures/5-3/double-exponential-quadrature-5.pdf} \\
		{\small Integral 5: $\int_0^\infty \frac{e^{-x}}{\sqrt{x}} dx$}
	\end{center}

\end{solution}

\begin{exercise}
	For each integral, compute approximations using the Gauss-Legendre quadrature
	rule with $n=4,6,8,\ldots,60$, and the proper double-exponential quadrature rule
	with $N=n/2$. Plot the errors as functions of $n$ on a semi-log scale.
	\begin{enumerate}
		\item $\int_{0}^1 \frac{1}{\sqrt{1-x^2}}\, dx = \dfrac{\pi}{2}$
		\item $\int_0^1 \sqrt{x} \log(x) \, dx = -\frac{4}{9}$
		\item $\int_0^1 \sqrt{1-x^2}\,\, dx = \frac{\pi}{4}$
		\item $\int_{0}^1 (\log x)^2\, dx = 2$
		\item $\int_{0}^{\pi/2} \log(\cos(x))\, dx = -\frac{\pi}{2}\log(2)$
		\item $\int_{0}^{\pi/2} \sqrt{\tan(x)}\, dx = \dfrac{\pi}{\sqrt{2}}$
	\end{enumerate}
\end{exercise}


\begin{solution}

	Standard Gaussian quadrature routines are derived under the assumption that
	the integrand is infinitely continuously differentiable ($C^\infty$) over the
	integration domain. When functions exhibit algebraic or logarithmic
	singularities at the boundary (e.g., $1/\sqrt{1-x^2}$ as $x \to 1$, or $(\log
		x)^2$ as $x \to 0$), the global smoothness assumption breaks down, severely
	degrading the GLQ convergence rate from exponential to merely algebraic.

	Conversely, Double-Exponential Quadrature restores spectral-like convergence
	by employing a nonlinear conformal mapping that flattens these boundary
	singularities. For a finite integration interval $(a, b)$, the standard
	tanh-sinh mapping is applied:
	\[
		x(t) = \frac{b-a}{2} \tanh\left(\frac{\pi}{2} \sinh(t)\right) + \frac{b+a}{2}
	\]
	This translates the physical domain $x \in (a, b)$ into an infinite
	computational domain $t \in (-\infty, \infty)$. The Jacobian of this
	transformation decays double-exponentially as $|t| \to \infty$, successfully
	overwhelming boundary singularities and permitting highly efficient truncation
	of the computational domain. To strictly ensure fairness between the two
	methodologies, the DE rule utilizes $N = n/2$ nodes, guaranteeing the total
	number of function evaluations remains theoretically balanced for direct
	comparison with GLQ.

	\paragraph{Results \& Discussion}
	Table \ref{tab:comparison-results} illustrates the final computed values at
	the maximum node density $n=60$.

	\begin{center}
		\centering
		\captionof{table}{Final evaluated approximations at $n=60$ ($N=30$ for DE).}
		\label{tab:comparison-results}
		\resizebox{\textwidth}{!}{
			\begin{tabular}{c r r r}
				\toprule
				\textbf{Integral}        & \textbf{Exact Solution} & \textbf{GLQ ($n=60$)} & \textbf{DEQ ($N=30$)} \\
				\midrule
				$\frac{1}{\sqrt{1-x^2}}$ & $1.5707963267948966$    & $1.5606199819857367$  & $1.5707963237302416$  \\
				$\sqrt{x} \log x$        & $-0.4444444444444444$   & $-0.4444486588707492$ & $-0.4444444444444445$ \\
				$\sqrt{1-x^2}$           & $0.7853981633974483$    & $0.7853988210355238$  & $0.7853981633974482$  \\
				$(\log x)^2$             & $2.0000000000000000$    & $1.9965066840331340$  & $2.0000000042167450$  \\
				$\log(\cos x)$           & $-1.0887930451518010$   & $-1.0885220739672092$ & $-1.0887930451517975$ \\
				$\sqrt{\tan x}$          & $2.2214414690791830$    & $2.2034051373383350$  & $2.2214414784702050$  \\
				\bottomrule
			\end{tabular}
		}
	\end{center}

	The numerical data confirms the theoretical limitations of Gauss-Legendre
	quadrature in the presence of singular endpoints. For functions possessing
	non-analytic bounds, GLQ converges slowly, struggling to achieve more than $2$
	to $5$ digits of accuracy even at $n=60$. By contrast, Double-Exponential
	quadrature consistently achieves near-machine precision ($\approx 10^{-15}$ to
	$10^{-9}$) with high efficiency, demonstrating
	unparalleled robustness for improper integrals.

	\textit{Note on computational evaluation:} During the evaluation of Integrals
	1 and 4, the DE algorithm yielded repeated bounds-decay warnings. This is an
	expected artifact of the extreme asymptotic behavior of these integrands.
	Because the pole $1/\sqrt{1-x^2} \to \infty$ as $x \to 1$ and the pole $(\log
		x)^2 \to \infty$ as $x \to 0$ grow exceptionally fast, they aggressively combat
	the boundary suppression provided by the tanh-sinh derivative.

	\paragraph{Figures}
	The error convergence profiles comparing GLQ and DEQ for all six integrals are
	presented below. Note the distinct qualitative difference: DEQ typically
	manifests a rapid, exponential descent, whereas GLQ exhibits a shallow, linear
	decay on the semi-logarithmic axes.

	\vspace{1em}
	\begin{center}
		\begin{tabular}{cc}
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-1.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-2.pdf}   \\
			{\small Integral 1: $\int_0^1 \frac{1}{\sqrt{1-x^2}} dx$}                   &
			{\small Integral 2: $\int_0^1 \sqrt{x} \log(x) dx$}                           \\[1.5em]
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-3.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-4.pdf}   \\
			{\small Integral 3: $\int_0^1 \sqrt{1-x^2} dx$}                             &
			{\small Integral 4: $\int_0^1 (\log x)^2 dx$}                                 \\[1.5em]
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-5.pdf} &
			\includegraphics[width=0.45\textwidth]{figures/5-3/comparison-gl-deq-6.pdf}   \\
			{\small Integral 5: $\int_0^{\pi/2} \log(\cos(x)) dx$}                      &
			{\small Integral 6: $\int_0^{\pi/2} \sqrt{\tan(x)} dx$}                       \\
		\end{tabular}
	\end{center}

\end{solution}
